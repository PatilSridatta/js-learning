# React + Next.js Interview Preparation

A 30-day learning and interview preparation journey focused on

JavaScript, React, Next.js, and building a scalable web application.

## Progress

| Day | Topic | Status |
|---|---|---|
| Day 1 | JavaScript Foundations | ✅ Completed |
| Day 2 | Array Methods | ✅ Completed |
| Day 3 | Reduce, Immutability & Spread | ✅ Completed |
| Day 4 | Promises & Async JavaScript | ⬜ Not Started |
| Day 5 | Event Loop | ⬜ Not Started |

## Day 1 — JavaScript Foundations

### Learned

- Scope
- `var`, `let`, `const`
- Reassignment vs mutation
- Functions
- Parameters vs arguments
- `return`
- Function expressions
- Arrow functions
- Object destructuring

### Project

- Set up React + Vite
- Created product data
- Imported product data into React
- Displayed product information

### Interview Questions

- `var` vs `let` vs `const`
- Function scope vs block scope
- Parameters vs arguments
- What does `return` do?
- What is an arrow function?
- What is destructuring?

### Git Commit

`Complete Day 1 JavaScript foundations`

---

## Day 2 — Array Methods

### Learned

- Arrays
- Arrays of objects
- Array indexing
- `forEach()`
- `map()`
- `filter()`
- `find()`
- Method chaining
- `filter()` + `map()`
- Rendering lists with `map()` in React
- React `key`

### Project

- Converted single product data into an array of products
- Rendered multiple products dynamically using `map()`
- Used `key={product.id}` when rendering product lists
- Practiced filtering products based on conditions
- Practiced chaining `filter()` and `map()`

### Interview Questions

- `forEach()` vs `map()`
- `map()` vs `filter()`
- `filter()` vs `find()`
- Why does React use `map()` to render lists?
- Why does React require a `key` when rendering lists?
- Can `filter()` and `map()` be chained?
- Why does `map()` return a new array?

### Git Commit

`Complete Day 2 array methods`